scalar Date

type Imc {
    id: ID!
    altura: String!
    peso: String!
    imc: String!
    dt_calculo: String!
}

type Query {
    historico(tamanho: Int): [Imc!]!
    historicoBetween(inicio: Date, fim: Date, tamanho: Int): [Imc!]!
}
