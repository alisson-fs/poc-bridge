scalar Instant

type Imc {
    id: ID!
    altura: String!
    peso: String!
    imc: String!
    data: String!
}

type Query {
    historico(tamanho: Int): [Imc!]!
    historicoBetween(inicio: Instant, fim: Instant, tamanho: Int): [Imc!]!
}
